docker run -ti --name test -p 82:80 debian /bin/bash
apt update
apt upgrade
apt install apache2
apt install php
apt install apt-transport-https lsb-release curl
curl -sSLo /usr/share/keyrings/deb.sury.org-php.gpg https://packages.sury.org/php/apt.gpg
echo "deb [signed-by=/usr/share/keyrings/deb.sury.org-php.gpg] https://packages.sury.org/php/ $(lsb_release -sc) main" > /etc/apt/sources.list.d/sury-php.list
apt update
apt autoremove php* libapache2-mod-php
apt install libapache2-mod-php8.1 php8.1
apt install php8.1-{curl,gd,intl,memcache,xml,zip,mbstring,mysql}
apt upgrade
apt install zip
php -r "copy('https://getcomposer.org/installer', '/root/composer-setup.php');"
php /root/composer-setup.php --install-dir="/usr/bin"
apt install git
git clone https://github.com/Blow07/ProjetSAE104.git /var/www/html/site/
cd /var/www/html/site
composer.phar -n update && composer.phar -n install
apt install lynx nano
nano /etc/apache2/sites-available/site.conf 
<VirtualHost *:80>
        ServerAdmin boyemouhamed43@gmail.com
        ServerName localhost
        DocumentRoot /var/www/html/site/public
        <Directory /var/www/html/site/public>
                AllowOverride None
                Order Allow,Deny
                Allow from All
        <IfModule mod_rewrite.c>
                RewriteEngine On
                RewriteCond %{REQUEST_URI} \..+$
                RewriteCond %{REQUEST_URI} !\.html$
                RewriteRule .* - [L]
                RewriteRule ^(.*)$ index.php [QSA,L]
        </IfModule>
        </Directory>
</VirtualHost>      
a2ensite site.conf 
a2dissite 000-default.conf
a2enmod rewrite 
service apache2 restart
chmod -R 770 /var/www/html/site/var/log
/usr/sbin/service apache2 start







